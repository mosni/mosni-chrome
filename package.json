{
  "name": "mosni-chrome",
  "version": "0.0.0",
  "private": true,
  "description": "mosnicat — the shared visual chrome / design system for the mosni stack",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "tsc --noEmit && tsc -p packages/react/tsconfig.json --noEmit && node scripts/build.mjs && node scripts/check-shape.mjs && node scripts/gen-react-icons.mjs --check && node scripts/gen-element-types.mjs --check && node scripts/parity.mjs && node scripts/react-behaviour.mjs && node scripts/smoke.mjs && prettier --check ."
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "lucide": "^1.23.0",
    "prettier": "^3.6.2",
    "prismjs": "^1.30.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "sass": "^1.101.0",
    "subset-font": "^2.5.0",
    "typescript": "^6.0.3"
  }
}
